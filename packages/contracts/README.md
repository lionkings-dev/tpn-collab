# @tpn/contracts

Internal shared contracts package for frontend and backend workspace apps.

Current scope:

- room ID validation contract (`room-id`)
- room API DTO contracts (`room-contracts`)
- auth API profile contract (`auth-contracts`)
- shared room/auth error code constants (`error-codes`)

Planned scope:

- additional API contracts as backend routes expand
