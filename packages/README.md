# Workspace Packages

This directory is reserved for shared workspace packages.

Current package:

- `contracts`: shared room-id contract used by frontend and backend.

Planned next additions:

- room/auth DTOs
- shared API error/status constants
